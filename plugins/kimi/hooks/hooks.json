{
  "hooks": {
    "kimi-review-gate": {
      "type": "Stop",
      "enabled": false,
      "description": "Before Claude stops, run a quick Kimi explore review on the response. If issues found, block the stop.",
      "command": "kimi:review-gate",
      "config": {
        "agent_file": "plugins/kimi/agent-files/explore.yaml",
        "prompt_template": "plugins/kimi/prompts/review-gate.md"
      }
    }
  }
}
